git clone https://github.com/Vholtninja/Kelompok-3
git add .
git commit -m "Hello"
git branch
git branch <cabang>
git checkout <cabang>
git merge <cabang>
git stash
git rebase <cabang>
git push origin <cabang>
git pull origin <cabang>
git add <add>
git commit -m "Resolve conflict"
echo "ignore" > .gitignore
git add .gitignore
git commit -m "Tambah .gitignore"


